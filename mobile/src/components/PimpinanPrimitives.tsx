import { ChevronRight, LayoutList, Plus, SlidersHorizontal } from "lucide-react-native";
import { Pressable, StyleSheet, Text, View } from "react-native";
import type { ReactNode } from "react";

import { colors, radii, shadows, spacing, typography } from "../theme";
import { showActionError } from "../lib/feedback";

export function Tabs({ items, active = 0 }: { items: string[]; active?: number }) { return <View style={styles.tabs}>{items.map((item, index) => <View key={item} style={[styles.tab, index === active && styles.activeTab]}><Text style={[styles.tabText, index === active && styles.activeTabText]}>{item}</Text></View>)}</View>; }
export function InteractiveTabs({ items, active, onChange }: { items: string[]; active: number; onChange: (index: number) => void }) { return <View style={styles.tabs}>{items.map((item, index) => <Pressable accessibilityRole="tab" accessibilityState={{ selected: index === active }} key={item} onPress={() => onChange(index)} style={({ pressed }) => [styles.tab, index === active && styles.activeTab, pressed && styles.pressed]}><Text style={[styles.tabText, index === active && styles.activeTabText]}>{item}</Text></Pressable>)}</View>; }
export function StatusPill({ children, tone = "green" }: { children: ReactNode; tone?: "green" | "red" | "orange" | "blue" | "gray" }) { return <Text style={[styles.pill, styles[`pill_${tone}`]]}>{children}</Text>; }
export function WorkflowStatusPill({ status }: { status: string }) { return <StatusPill tone={statusTone(status)}>{status}</StatusPill>; }
export function statusTone(status: string): "green" | "red" | "orange" | "blue" | "gray" {
  const value = status.toLowerCase();
  if (/(reject|tolak|gagal|overdue|tunggak|failed|blocked)/.test(value)) return "red";
  if (/(draft|pending|menunggu|overdue|jatuh tempo)/.test(value)) return "orange";
  if (/(submit|ajukan|verify|verifikasi|sync|sinkron)/.test(value)) return "blue";
  if (/(approve|approved|sah|paid|dibayar|lunas|success|berhasil)/.test(value)) return "green";
  return "gray";
}
export function Card({ children, style }: { children: ReactNode; style?: object }) { return <View style={[styles.card, style]}>{children}</View>; }
export function MetricCard({ label, value, hint, tone = "blue" }: { label: string; value: string; hint?: string; tone?: "green" | "red" | "orange" | "blue" }) { return <Card style={styles.metricCard}><View style={[styles.metricAccent, styles[`metricAccent_${tone}`]]} /><Text style={styles.metricLabel}>{label}</Text><Text numberOfLines={1} adjustsFontSizeToFit style={styles.metricValue}>{value}</Text>{hint ? <Text style={styles.metricHint}>{hint}</Text> : null}</Card>; }
export function FilterChip({ label, active = false, onPress }: { label: string; active?: boolean; onPress?: () => void }) { const content = <><SlidersHorizontal color={active ? colors.primary : colors.textMuted} size={13} /><Text style={[styles.filterText, active && styles.filterTextActive]}>{label}</Text></>; return onPress ? <Pressable accessibilityRole="button" onPress={onPress} style={({ pressed }) => [styles.filterChip, active && styles.filterChipActive, pressed && styles.pressed]}>{content}</Pressable> : <View style={[styles.filterChip, active && styles.filterChipActive]}>{content}</View>; }
export function ActionButton({ children, onPress }: { children: ReactNode; onPress: () => void | Promise<void> }) {
  async function handlePress() {
    try { await onPress(); } catch (error) { showActionError(error); }
  }
  return <Pressable accessibilityRole="button" onPress={() => void handlePress()} style={({ pressed }) => [styles.action, pressed && styles.pressed]}><Plus color="#FFFFFF" size={16} /><Text style={styles.actionText}>{children}</Text></Pressable>;
}
export function RowCard({ title, subtitle, meta, status, tone = "green", icon, thumbnail, onPress }: { title: string; subtitle?: string; meta?: string; status?: string; tone?: "green" | "red" | "orange" | "blue" | "gray"; icon?: ReactNode; thumbnail?: ReactNode; onPress?: () => void | Promise<void> }) {
  const content = <><View style={styles.rowIcon}>{thumbnail ?? icon}</View><View style={styles.rowCopy}><Text style={styles.rowTitle}>{title}</Text>{subtitle ? <Text style={styles.rowSubtitle}>{subtitle}</Text> : null}{meta ? <Text style={styles.rowMeta}>{meta}</Text> : null}</View>{status ? <StatusPill tone={tone}>{status}</StatusPill> : null}{onPress ? <ChevronRight color="#98A2B3" size={16} /> : null}</>;
  if (!onPress) return <View style={styles.rowCard}>{content}</View>;
  return <Pressable accessibilityRole="button" onPress={() => void onPress()} style={({ pressed }) => [styles.rowCard, pressed && styles.pressed]}>{content}</Pressable>;
}
export function SectionTitle({ children, action, icon }: { children: ReactNode; action?: string; icon?: ReactNode }) { return <View style={styles.section}><View style={{ alignItems: "center", flexDirection: "row", gap: 7 }}><View style={{ alignItems: "center", backgroundColor: colors.primarySoft, borderRadius: radii.sm, height: 28, justifyContent: "center", width: 28 }}>{icon ?? <LayoutList color={colors.primary} size={15} strokeWidth={2.2} />}</View><Text style={styles.sectionTitle}>{children}</Text></View>{action ? <Text style={styles.sectionAction}>{action}</Text> : null}</View>; }
const styles = StyleSheet.create({ tabs: { backgroundColor: colors.surfaceMuted, borderColor: colors.border, borderRadius: radii.lg, borderWidth: 1, flexDirection: "row", padding: 3 }, tab: { alignItems: "center", flex: 1, justifyContent: "center", minHeight: 44, paddingHorizontal: 5, paddingVertical: 8 }, activeTab: { backgroundColor: colors.surface, borderColor: colors.border, borderRadius: radii.md, borderWidth: 1, ...shadows.card }, tabText: { color: colors.textMuted, fontSize: typography.caption, fontWeight: "700", lineHeight: 13, textAlign: "center" }, activeTabText: { color: colors.primary, fontWeight: "900" }, pressed: { opacity: 0.76 }, card: { backgroundColor: colors.surface, borderColor: colors.border, borderRadius: radii.lg, borderWidth: 1, padding: spacing.md, ...shadows.card }, metricCard: { flex: 1, minHeight: 96, overflow: "hidden", paddingLeft: spacing.md, paddingTop: spacing.sm }, metricAccent: { borderRadius: radii.pill, height: 5, marginBottom: 8, width: 26 }, metricAccent_green: { backgroundColor: colors.finance }, metricAccent_red: { backgroundColor: colors.danger }, metricAccent_orange: { backgroundColor: colors.warning }, metricAccent_blue: { backgroundColor: colors.primary }, metricLabel: { color: colors.textMuted, fontSize: typography.micro, fontWeight: "800", textTransform: "uppercase" }, metricValue: { color: colors.textStrong, fontSize: 18, fontWeight: "900", marginTop: 4 }, metricHint: { color: colors.textSubtle, fontSize: typography.micro, marginTop: 3 }, filterChip: { alignItems: "center", backgroundColor: colors.surface, borderColor: colors.border, borderRadius: radii.pill, borderWidth: 1, flexDirection: "row", gap: 5, minHeight: 36, paddingHorizontal: 12 }, filterChipActive: { backgroundColor: colors.primarySoft, borderColor: colors.primary }, filterText: { color: colors.textMuted, fontSize: typography.caption, fontWeight: "800" }, filterTextActive: { color: colors.primary }, pill: { borderRadius: radii.pill, fontSize: typography.micro, fontWeight: "800", overflow: "hidden", paddingHorizontal: 8, paddingVertical: 5 }, pill_green: { backgroundColor: colors.successSoft, color: colors.success }, pill_red: { backgroundColor: colors.dangerSoft, color: colors.danger }, pill_orange: { backgroundColor: colors.warningSoft, color: colors.warning }, pill_blue: { backgroundColor: colors.primarySoft, color: colors.primary }, pill_gray: { backgroundColor: colors.surfaceMuted, color: colors.textMuted }, action: { alignItems: "center", backgroundColor: colors.primary, borderRadius: radii.md, flexDirection: "row", gap: 7, justifyContent: "center", minHeight: 48, padding: 13 }, actionText: { color: "#FFFFFF", fontSize: typography.body, fontWeight: "800" }, rowCard: { alignItems: "center", backgroundColor: colors.surface, borderColor: colors.border, borderRadius: radii.lg, borderWidth: 1, flexDirection: "row", gap: spacing.sm, minHeight: 72, padding: spacing.sm, ...shadows.card }, rowIcon: { alignItems: "center", backgroundColor: colors.surfaceStrong, borderRadius: radii.md, height: 46, justifyContent: "center", width: 52 }, rowCopy: { flex: 1 }, rowTitle: { color: colors.textStrong, fontSize: typography.body, fontWeight: "800" }, rowSubtitle: { color: colors.textMuted, fontSize: typography.caption, marginTop: 4 }, rowMeta: { color: colors.textMuted, fontSize: typography.micro, marginTop: 4 }, section: { alignItems: "center", flexDirection: "row", justifyContent: "space-between", marginTop: spacing.xs }, sectionTitle: { color: colors.textStrong, fontSize: typography.section, fontWeight: "800" }, sectionAction: { color: colors.primary, fontSize: typography.caption, fontWeight: "800" } });
