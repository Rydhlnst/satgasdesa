import { ActivityIndicator, Pressable, RefreshControl, ScrollView, StyleSheet, Text, View } from "react-native";
import type { ReactNode } from "react";
import { useRouter } from "expo-router";
import { useQuery } from "@tanstack/react-query";
import { Bell, CircleUserRound, ClipboardCheck, FileText, Home, MapPinned, Menu, Search, TrendingUp, WalletCards } from "lucide-react-native";
import { SafeAreaView } from "react-native-safe-area-context";

import { useAuth } from "../auth";
import { getNotifications } from "../lib/api";
import { colors, roleTheme, spacing, type Role } from "../theme";

export function Screen({ children, refreshing, onRefresh, scroll = true }: { children: ReactNode; refreshing?: boolean; onRefresh?: () => void; scroll?: boolean }) {
  const content = scroll ? <ScrollView contentContainerStyle={styles.content} refreshControl={onRefresh ? <RefreshControl onRefresh={onRefresh} refreshing={Boolean(refreshing)} tintColor={colors.primary} /> : undefined}>{children}</ScrollView> : <View style={styles.content}>{children}</View>;
  return <SafeAreaView edges={["top", "bottom"]} style={styles.safe}>{content}</SafeAreaView>;
}

export function Header({ role, title, subtitle }: { role: Role; title: string; subtitle?: string }) {
  const { session } = useAuth();
  const router = useRouter();
  const notifications = useQuery({ queryKey: ["notifications", "unread-count"], queryFn: () => getNotifications({ unreadOnly: true, pageSize: 1 }) });
  const theme = roleTheme[role];
  return <><View style={[styles.header, { backgroundColor: theme.header }]}><View style={styles.headerSide}><Menu color="#FFFFFF" size={21} /><View><Text style={styles.headerEyebrow}>SATGAS DESA SEJOLI</Text><Text style={styles.headerTitle}>{theme.label}</Text><Text style={styles.headerSubtitle}>{subtitle ?? "Akses operasional berbasis peran"}</Text></View></View><View style={styles.headerActions}><Pressable onPress={() => router.push("/notifications")} hitSlop={10} style={styles.bell}><Bell color="#FFFFFF" size={19} />{notifications.data?.total ? <View style={styles.notificationBadge}><Text style={styles.notificationBadgeText}>{notifications.data.total > 9 ? "9+" : notifications.data.total}</Text></View> : null}</Pressable><Pressable onPress={() => router.push("/profile")} hitSlop={10}><CircleUserRound color="#FFFFFF" size={24} /><Text style={styles.userName}>{session?.user.name}</Text></Pressable></View></View><View style={styles.pageBar}><Text style={styles.pageTitle}>{title}</Text></View></>;
}

export function LoadingState() { return <View style={styles.state}><ActivityIndicator color={colors.primary} /><Text style={styles.muted}>Memuat data…</Text></View>; }
export function ErrorState({ message, onRetry }: { message: string; onRetry: () => void }) { return <View style={styles.state}><Text style={styles.errorTitle}>Terjadi masalah</Text><Text style={styles.muted}>{message}</Text><Pressable onPress={onRetry} style={styles.retry}><Text style={styles.retryText}>Coba lagi</Text></Pressable></View>; }
export function EmptyState({ message }: { message: string }) { return <View style={styles.state}><Text style={styles.muted}>{message}</Text></View>; }
export function BottomNav({ role, current }: { role: Role; current: string }) {
  if (role === "PIMPINAN") return <View style={styles.nav}><NavItem icon={Home} label="Dashboard" active={current === "dashboard"} href="/dashboard" /><NavItem icon={MapPinned} label="Monitoring Blok" active={current === "monitoring"} href="/monitoring" /><NavItem icon={WalletCards} label="Keuangan" active={current === "finance"} href="/finance" /><NavItem icon={Search} label="Anggaran" active={current === "budgets"} href="/budgets" /><NavItem icon={FileText} label="Laporan" active={current === "reports" || current === "realizations"} href="/reports" /></View>;
  if (role === "BENDAHARA") return <View style={styles.nav}><NavItem icon={Home} label="Dashboard" active={current === "dashboard"} href="/dashboard" /><NavItem icon={WalletCards} label="Keuangan" active={current === "finance"} href="/finance" /><NavItem icon={Search} label="Anggaran" active={current === "budgets"} href="/budgets" /><NavItem icon={ClipboardCheck} label="Pengajuan" active={current === "proposals"} href="/proposals" /><NavItem icon={TrendingUp} label="Realisasi" active={current === "realizations"} href="/realizations" /><NavItem icon={FileText} label="Laporan" active={current === "reports"} href="/reports" /></View>;
  if (role === "PETUGAS_LAPANGAN") return <View style={styles.nav}><NavItem icon={Home} label="Dashboard" active={current === "dashboard"} href="/dashboard" /><NavItem icon={FileText} label="Pemeriksaan" active={current === "inspections" || current === "monitoring" || current === "map"} href="/monitoring" /><NavItem icon={MapPinned} label="Excavator" active={current === "excavators"} href="/excavators" /><NavItem icon={WalletCards} label="Informasi" active={current === "information"} href="/information" /><NavItem icon={CircleUserRound} label="Profil" active={current === "profile"} href="/profile" /></View>;
  return <View style={styles.nav}><NavItem icon={Home} label="Beranda" active={current === "dashboard"} href="/dashboard" /><NavItem icon={MapPinned} label="Keuangan" active={current === "monitoring"} href="/finance" /><NavItem icon={WalletCards} label="Informasi" active={false} href="/information" /><NavItem icon={Search} label="Laporan" active={false} href="/reports" /><NavItem icon={CircleUserRound} label="Akun" active={false} href="/dashboard" /></View>;
}

function NavItem({ label, active, href, icon: Icon }: { label: string; active: boolean; href: "/dashboard" | "/monitoring" | "/finance" | "/information" | "/reports" | "/budgets" | "/proposals" | "/realizations" | "/inspections" | "/excavators" | "/profile" | "/notifications"; icon: typeof Home }) {
  const router = useRouter();
  return <Pressable onPress={() => router.push(href)} style={styles.navItem}><Icon color={active ? colors.primary : colors.textMuted} size={18} /><Text style={[styles.navLabel, active && styles.navActive]}>{label}</Text></Pressable>;
}

const styles = StyleSheet.create({ safe: { backgroundColor: colors.page, flex: 1 }, content: { gap: spacing.lg, padding: spacing.md, paddingBottom: 90 }, header: { alignItems: "center", flexDirection: "row", justifyContent: "space-between", paddingHorizontal: spacing.md, paddingVertical: 12 }, headerSide: { alignItems: "center", flexDirection: "row", gap: 10 }, headerActions: { alignItems: "center", flexDirection: "row", gap: 14 }, bell: { position: "relative" }, notificationBadge: { alignItems: "center", backgroundColor: "#E63630", borderColor: "#FFFFFF", borderRadius: 10, borderWidth: 1, height: 16, justifyContent: "center", minWidth: 16, paddingHorizontal: 3, position: "absolute", right: -8, top: -7 }, notificationBadgeText: { color: "#FFFFFF", fontSize: 8, fontWeight: "900" }, headerEyebrow: { color: "#FFFFFF", fontSize: 10, fontWeight: "800", letterSpacing: 0.4 }, headerTitle: { color: "#FFE34F", fontSize: 10, fontWeight: "900", marginTop: 4 }, headerSubtitle: { color: "#DCE7FF", fontSize: 9, marginTop: 3 }, userName: { color: "#FFFFFF", fontSize: 8, fontWeight: "700", marginTop: 2, maxWidth: 65, textAlign: "center" }, pageBar: { backgroundColor: "#FFFFFF", borderBottomColor: colors.border, borderBottomWidth: 1, paddingHorizontal: spacing.md, paddingVertical: 12 }, pageTitle: { color: colors.text, fontSize: 14, fontWeight: "900", textTransform: "uppercase" }, state: { alignItems: "center", gap: spacing.sm, justifyContent: "center", minHeight: 200 }, muted: { color: colors.textMuted, fontSize: 12, textAlign: "center" }, errorTitle: { color: colors.danger, fontSize: 15, fontWeight: "800" }, retry: { backgroundColor: colors.primary, borderRadius: 8, paddingHorizontal: 16, paddingVertical: 10 }, retryText: { color: "#fff", fontSize: 12, fontWeight: "700" }, nav: { backgroundColor: "#FFFFFF", borderColor: colors.border, borderTopWidth: 1, bottom: 0, flexDirection: "row", left: 0, paddingBottom: 10, paddingTop: 8, position: "absolute", right: 0 }, navItem: { alignItems: "center", flex: 1, gap: 3 }, navLabel: { color: colors.textMuted, fontSize: 8, fontWeight: "700", textAlign: "center" }, navActive: { color: colors.primary } });
