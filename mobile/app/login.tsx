import { zodResolver } from "@hookform/resolvers/zod";
import { Link, Redirect, useRouter } from "expo-router";
import { useState } from "react";
import { Controller, useForm } from "react-hook-form";
import { Eye, EyeOff } from "lucide-react-native";
import { ActivityIndicator, KeyboardAvoidingView, Platform, Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { z } from "zod";

import { useAuth } from "../src/auth";
import { loginSchema as schema } from "../src/form-schemas";
import { showActionError } from "../src/lib/feedback";
import { colors, radii, shadows, spacing, typography } from "../src/theme";

type FormValues = z.infer<typeof schema>;

export default function Login() {
  const { loading, session, signIn } = useAuth();
  const router = useRouter();
  const [serverError, setServerError] = useState<string | null>(null);
  const [showPassword, setShowPassword] = useState(false);
  const { control, handleSubmit, formState: { errors, isSubmitting } } = useForm<FormValues>({ resolver: zodResolver(schema), mode: "onBlur", reValidateMode: "onChange", defaultValues: { email: "", password: "" } });
  if (!loading && session) return <Redirect href="/dashboard" />;
  async function submit(values: FormValues) { setServerError(null); try { await signIn(values.email, values.password); router.replace("/dashboard"); } catch (error) { const message = error instanceof Error ? error.message : "Tidak dapat masuk."; setServerError(message); showActionError(error, message); } }
  return <KeyboardAvoidingView style={styles.page} behavior={Platform.OS === "ios" ? "padding" : undefined}><View style={styles.brandPanel}><View style={styles.brandGlow} /><View style={styles.brandMark}><Text style={styles.brandMarkText}>S</Text></View><Text style={styles.brandTitle}>SATGAS DESA</Text><Text style={styles.brandSubtitle}>SEJOLI</Text><Text style={styles.brandHint}>Operasional desa dalam satu ruang kerja</Text></View><View style={styles.card}><Text style={styles.eyebrow}>AKSES INTERNAL</Text><Text style={styles.title}>Masuk</Text><Text style={styles.description}>Gunakan akun internal yang diberikan administrator.</Text><Text style={styles.label}>Email</Text><Controller control={control} name="email" render={({ field: { onChange, onBlur, value } }) => <TextInput autoCapitalize="none" autoComplete="email" keyboardType="email-address" onBlur={onBlur} onChangeText={onChange} placeholder="nama@contoh.id" placeholderTextColor={colors.textMuted} style={styles.input} value={value} />} />{errors.email ? <Text style={styles.error}>{errors.email.message}</Text> : null}<Text style={styles.label}>Kata sandi</Text><View style={styles.inputWrap}><Controller control={control} name="password" render={({ field: { onChange, onBlur, value } }) => <TextInput autoComplete="password" onBlur={onBlur} onChangeText={onChange} placeholder="Kata sandi" placeholderTextColor={colors.textMuted} secureTextEntry={!showPassword} style={[styles.input, styles.inputWithAction]} value={value} />} /><Pressable accessibilityLabel={showPassword ? "Hide password" : "Show password"} accessibilityRole="button" hitSlop={8} onPress={() => setShowPassword((current) => !current)} style={styles.inputAction}>{showPassword ? <EyeOff color={colors.textMuted} size={19} /> : <Eye color={colors.textMuted} size={19} />}</Pressable></View>{errors.password ? <Text style={styles.error}>{errors.password.message}</Text> : null}{serverError ? <Text style={styles.error}>{serverError}</Text> : null}<Pressable disabled={isSubmitting} onPress={handleSubmit(submit)} style={({ pressed }) => [styles.button, pressed && { opacity: 0.8 }]}>{isSubmitting ? <ActivityIndicator color="#fff" /> : <Text style={styles.buttonText}>Masuk</Text>}</Pressable><Link href="/forgot-password" style={styles.forgot}>Lupa kata sandi?</Link></View></KeyboardAvoidingView>;
}

const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: colors.page, justifyContent: "center", padding: spacing.lg }, brandPanel: { backgroundColor: colors.primaryDark, borderRadius: radii.xl, marginBottom: spacing.md, minHeight: 205, overflow: "hidden", padding: spacing.xl, ...shadows.card }, brandGlow: { backgroundColor: colors.finance, borderRadius: 130, height: 220, opacity: 0.35, position: "absolute", right: -70, top: -90, width: 220 }, brandMark: { alignItems: "center", backgroundColor: "rgba(255,255,255,0.18)", borderColor: "rgba(255,255,255,0.35)", borderRadius: 15, borderWidth: 1, height: 50, justifyContent: "center", marginBottom: spacing.lg, width: 50 }, brandMarkText: { color: "#FFFFFF", fontSize: 27, fontWeight: "900" }, brandTitle: { color: "#FFFFFF", fontSize: 25, fontWeight: "900", letterSpacing: 1 }, brandSubtitle: { color: "#CFE0FF", fontSize: 14, fontWeight: "800", letterSpacing: 5, marginTop: 2 }, brandHint: { color: "#DCE7FF", fontSize: typography.caption, marginTop: spacing.md }, card: { backgroundColor: colors.surface, borderColor: colors.border, borderRadius: radii.xl, borderWidth: 1, padding: spacing.lg, ...shadows.card }, eyebrow: { color: colors.primary, fontSize: typography.micro, fontWeight: "900", letterSpacing: 0.8 }, title: { color: colors.textStrong, fontSize: 25, fontWeight: "900", marginTop: spacing.xs }, description: { color: colors.textMuted, fontSize: typography.body, lineHeight: 20, marginBottom: spacing.lg, marginTop: spacing.sm }, label: { color: colors.textStrong, fontSize: typography.caption, fontWeight: "800", marginBottom: 7, marginTop: spacing.md }, inputWrap: { position: "relative" }, input: { backgroundColor: colors.surface, borderColor: colors.border, borderRadius: radii.md, borderWidth: 1, color: colors.textStrong, height: 48, paddingHorizontal: 14 }, inputWithAction: { paddingRight: 48 }, inputAction: { alignItems: "center", height: 46, justifyContent: "center", position: "absolute", right: 2, top: 1, width: 44 }, error: { color: colors.danger, fontSize: typography.micro, marginTop: 6 }, button: { alignItems: "center", backgroundColor: colors.primary, borderRadius: radii.md, height: 50, justifyContent: "center", marginTop: spacing.xl }, buttonText: { color: "#fff", fontSize: typography.body, fontWeight: "900" }, forgot: { color: colors.primary, fontSize: typography.caption, fontWeight: "800", marginTop: spacing.md, textAlign: "center" } });
